{
  "name": "quiet",
  "version": "0.2.0",
  "description": "A Tistory skin for writing you want taken seriously.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:preview": "node scripts/build.mjs --preview",
    "preview": "node scripts/build.mjs --preview --serve",
    "check": "node scripts/check.mjs",
    "release": "node scripts/release.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.25.0",
    "lightningcss": "^1.27.0",
    "nunjucks": "^3.2.4"
  }
}
