{% extends "layout.njk" %}
{% import "macros.njk" as m %}
{% block main %}
{% set a = article %}
<article id="sw-article">
  <header class="sw-article-head">
    <div class="cover" aria-hidden="true"></div>
    <div class="inner">
      <a class="sw-crumb" href="{{ a.categoryUrl }}">{{ a.crumb }}</a>
      <h1>{{ a.title }}</h1>
      <div class="sw-byline">
        <span>{{ a.date }}</span><span>{{ a.readtime }}</span><span>Comments {{ a.comments }}</span>
        <span class="admin"><a href="#">Edit</a> · <a href="#">Delete</a></span>
      </div>
    </div>
  </header>

  <div class="sw-article-body">
    {{ a.body | safe }}
  </div>

  <div class="sw-article-foot">
    <div class="sw-tags">
      {% for t in a.tags %}<a href="/tag/{{ t }}">#{{ t }}</a>{% endfor %}
    </div>

    <div class="sw-related">
      <div class="sw-section-tag"><h2>More in '{{ a.categoryName }}'</h2></div>
      <ul>
        {% for r in a.related %}<li><a href="{{ r.url }}"><span>{{ r.title }}</span><time>{{ r.date }}</time></a></li>{% endfor %}
      </ul>
    </div>

    <div class="sw-author">
      <div class="sw-monogram" aria-hidden="true">{{ site.initial }}</div>
      <div><div class="n">{{ site.name }}</div><div class="b">{{ site.bio }}</div></div>
    </div>
  </div>

  <section class="sw-comments" aria-label="Comments">
    <div class="sw-section-tag"><h2>Comments {{ a.comments }}</h2></div>
    <form class="sw-cbox" onsubmit="return false">
      <label class="sw-sr" for="sw-comment">Write a comment</label>
      <textarea id="sw-comment" placeholder="Leave a comment"></textarea>
      <div class="cfields">
        <label class="sw-sr" for="sw-cname">Name</label><input id="sw-cname" placeholder="Name" autocomplete="name">
        <label class="sw-sr" for="sw-cpw">Password</label><input id="sw-cpw" type="password" placeholder="Password" autocomplete="off">
        <button type="submit" class="submit">Post</button>
      </div>
    </form>
    {% for c in a.commentList %}{{ m.comment(c) }}{% endfor %}
  </section>
</article>
{% endblock %}
