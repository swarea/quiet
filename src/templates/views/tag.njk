{% extends "layout.njk" %}
{% block main %}
<main id="sw-tag">
  <header class="sw-article-head">
    <div class="cover" aria-hidden="true"></div>
    <div class="inner">
      <a class="sw-crumb" href="/">swarea</a>
      <h1>Tags</h1>
      <div class="sw-byline"><span>{{ tags.length }} tags</span></div>
    </div>
  </header>
  <div class="sw-wrap">
    <section class="sw-tagcloud" aria-label="Tags">
      <ul>
        {% for t in tags %}
        <li><a href="/tag/{{ t.name }}"><span class="name">{{ t.name }}</span><span class="n">{{ t.count }}</span></a></li>
        {% endfor %}
      </ul>
    </section>
  </div>
</main>
{% endblock %}
